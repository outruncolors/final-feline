import { MdAccountBalance } from "react-icons/md";
import { ComposableMenu } from "./ComposableMenu";

export const EntityMenu = () => (
  <ComposableMenu title="Entity" Icon={MdAccountBalance}>
    Entity
  </ComposableMenu>
);
